[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
# 저장소 루트를 import 루트로 둔다 → 테스트가 `app.*` 정식 패키지 경로로 import 한다
# (과거 conftest 의 sys.path 해킹을 대체).
pythonpath = .
